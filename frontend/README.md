# MF Enterprise Frontend

Create React App frontend for the MF Enterprise public website.

## Scripts

- `npm start` runs the development server.
- `npm run build` creates the static production site in `build/`.
- `npm test` runs the React test runner.
